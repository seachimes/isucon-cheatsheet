# 計測基盤の共有サーバ（Akamai/Linode）。全環境で1台を使い回す。
# SSH は Cloudflare Tunnel 経由（cloudflared access ssh）。
#   -i inventories/pprotein_server
[pprotein_server]
pprotein ansible_host=pprotein-ssh.wtnqk.org ansible_user=root ansible_ssh_private_key_file=~/.ssh/id_akamai
